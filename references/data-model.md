# Data Model

兼容旧引用的入口文件。

`feature-doc-pack` 当前不再使用 `DATA-MODEL.md` 作为默认数据库协作文档。涉及数据库变更、数据模型、表结构、字段、索引、数据关系、数据迁移、历史数据兼容或需要持久化的数据结构时，应读取 [database-design.md](database-design.md)，并生成或更新 `DATABASE-DESIGN.md`。

保留本文件只是为了避免旧引用失效；新增规则不要继续写入这里。
